# Python
__pycache__/
*.pyc
.venv/
venv/

# Verification oracle artifacts — these contain code extracted from the locally
# installed Claude.app and MUST NOT be committed (proprietary). Regenerated on demand.
oracle.js
oracle*.json
/tmp/
*.frozen/
extracted-app/

# OS / editor
.DS_Store
node_modules/

# Local working notes — not for the public repo
docs/prompts/
.gstack/

# Generated per-user data — lives on each fork's `output` branch, never on the code branch.
# data/stats.json stays on disk locally: it is the accumulation state (see tools/publish.sh).
data/
docs/data/
assets/
.output/
